import type { Size } from "./typography";

/**
 * Base typography sizes.
 * Adjust these to scale the entire system.
 */
export const typographyDefaults = {
  text: 3 as Size,
  heading: 6 as Size,
};
